package server;

/**
 * The Server interface defines the methods for starting and stopping a server.
 */
public interface Server {

     /**
      * Starts the server.
      */
     void start();

     /**
      * Stops the server.
      */
     void stop();
}
